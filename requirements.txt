# Snowball AI/OS core runtime dependencies
#
# Keep this file intentionally small.
# Optional systems such as embeddings, Cosmos, voice, games, and robotics
# should use separate dependency groups when they are added back.

cachetools==5.5.1
requests==2.32.3
fastapi==0.136.3
pydantic==2.10.6
uvicorn==0.34.0